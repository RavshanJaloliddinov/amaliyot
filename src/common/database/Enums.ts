export enum Roles {
    ADMIN = "admin",
    USER = "user",
    SUPER_ADMIN = "super_admin",
}

export enum ProductStatus {
    AVAILABLE = 'available',
    OUT_OF_STOCK = 'out_of_stock',
    DISCONTINUED = 'discontinued',
}
